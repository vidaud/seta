export default PopupWindow;

interface PopupWindowOptions {
  height: number
  width: number
  top: number
  left: number
}

declare class PopupWindow {
  static open(id: string, url: string, options?: PopupWindowOptions): PopupWindow;
  constructor(id: string, url: string, options?: PopupWindowOptions);
  id: string;
  url: string;
  options: PopupWindowOptions | {};
  open(id: string, url: string, options?: PopupWindowOptions): void;
  window: Window;
  close(): void;
  poll(): void;
  promise: Promise<{ code: string }>;
  _iid: number;
  cancel(): void;
  then(onSuccess: (data: { code: string }) => void, onFailure: (error: Error) => void): Promise<{ code: string }>;
}
